import React from "react";
import Logo from "../assets/background1.svg"

export default function Head() {
    return (
        <section className="flex-col bg-fixed w-full h-96" style={{backgroundImage: `url(${Logo})` }}>
            <div>

            </div>

            <div>

            </div>
        </section>
    )
}